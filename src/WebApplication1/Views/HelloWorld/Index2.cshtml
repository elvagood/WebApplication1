@model IEnumerable<WebApplication1.Models.ZJFI_TempVM>
@{
    ViewData["Title"] = "Hello World";
}
<h2>Hello World</h2>




<table class="table table-bordered table-hover">
    @foreach (var item in Model)
    {
    <tr>
        <td>@item.site_ref</td>
        <td>@item.Desc</td>
        <td>@item.Test</td>
    </tr>
    }
</table>
